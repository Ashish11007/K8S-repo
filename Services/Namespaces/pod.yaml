apiVersion: v1
kind: Pod
metadata:
  name: nppod
  labels:
    app: myapp
spec: 
  containers:
    - name: nginx-containers
      namespace: nginx
      image: nginx:latest
      ports:
        - name: nginx-port
          containerPort: 80