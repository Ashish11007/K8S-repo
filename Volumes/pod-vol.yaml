apiVersion: v1 
kind: Pod
metadata:
 name: mypod
 labels:
 app: myapp 
spec:
 volumes:
 - name: my-vol
 persistentVolumeClaim:
 claimName: mypvc
 containers:
 - name: myapp
 image: nginx:latest
 ports:
 - containerPort: 80 
 volumeMounts:
 - mountPath: /usr/share/nginx/html/
 name: my-vol