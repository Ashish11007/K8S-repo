apiVersion: v1
kind: Service
metadata:
  name: my-svc
  label:
    app: myapp
  namespace: hpa
spec:
  type: ClusterIP
  selector:
    app: myapp
  ports:
    - name: my-svc-port
      port: 80
      targetPort: 80